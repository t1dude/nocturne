{
  "name": "Nocturne",
  "description": "Nightscout-compatible diabetes management API with Row Level Security and multi-tenancy.",
  "longDescription": "<p>Nocturne is a modern .NET rewrite of the Nightscout diabetes management API with full v1/v2/v3 API compatibility. It adds multi-tenancy with PostgreSQL Row Level Security, a SvelteKit web UI, and optional chat bot integrations (Discord, Slack, Telegram, WhatsApp).</p><p><strong>One-time bootstrap required after first deployment.</strong> Nocturne uses three separate PostgreSQL roles for security. Run the following in a terminal on your Cosmos host, substituting your generated passwords from the container environment variables:</p><pre>docker exec -i &lt;name&gt;-postgres psql -U nocturne -d nocturne &lt;&lt;'SQL'\nCREATE ROLE nocturne_migrator LOGIN NOSUPERUSER NOBYPASSRLS NOCREATEDB NOCREATEROLE PASSWORD '&lt;MIGRATOR_PASSWORD&gt;';\nCREATE ROLE nocturne_app LOGIN NOSUPERUSER NOBYPASSRLS NOCREATEDB NOCREATEROLE PASSWORD '&lt;APP_PASSWORD&gt;';\nCREATE ROLE nocturne_web LOGIN NOSUPERUSER NOBYPASSRLS NOCREATEDB NOCREATEROLE PASSWORD '&lt;WEB_PASSWORD&gt;';\nALTER DATABASE nocturne OWNER TO nocturne_migrator;\nALTER SCHEMA public OWNER TO nocturne_migrator;\nGRANT CONNECT ON DATABASE nocturne TO nocturne_app;\nGRANT USAGE ON SCHEMA public TO nocturne_app;\nGRANT CONNECT ON DATABASE nocturne TO nocturne_web;\nGRANT USAGE, CREATE ON SCHEMA public TO nocturne_web;\nALTER DEFAULT PRIVILEGES FOR ROLE nocturne_migrator IN SCHEMA public GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO nocturne_app;\nALTER DEFAULT PRIVILEGES FOR ROLE nocturne_migrator IN SCHEMA public GRANT USAGE, SELECT ON SEQUENCES TO nocturne_app;\nSQL</pre><p>Then restart the <code>-api</code> and <code>-web</code> containers. You must also set <code>BASE_DOMAIN</code> on both and <code>ORIGIN=https://&lt;your-domain&gt;</code> on <code>-web</code>.</p><p>Configure data connectors and chat bot credentials through Settings → Administration after first login.</p>",
  "tags": ["health", "diabetes", "nightscout", "cgm", "healthcare"],
  "repository": "https://github.com/nightscout/nocturne",
  "image": "https://ghcr.io/nightscout/nocturne/nocturne-api",
  "supported_architectures": ["amd64", "arm64"]
}
